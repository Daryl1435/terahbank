# Background job workers and scheduler registration.
# Jobs run via BullMQ (Redis-backed). Workers run as separate processes in production.
# To start workers locally:
#   python -m modules.jobs.worker
